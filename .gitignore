# dependencies
node_modules/

# build artifacts
dist/
*.wasm
*.mjs
# ...but the playground's static server is hand-written source, not build output
!playground/serve.mjs

# editor / tooling local state
.claude/settings.local.json
*.vsix
