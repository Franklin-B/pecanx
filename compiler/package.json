{
  "name": "pcx",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": { "pcx": "./pcx.js" },
  "description": "The PecanX compiler (pcx) — v0.2: lexer, parser, exhaustiveness + whole-program Hindley-Milner type inference, multi-module linker, and JavaScript / WebAssembly (WasmGC) / real-DOM backends.",
  "scripts": {
    "test": "node tests/run.js"
  }
}
